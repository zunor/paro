DROP FUNCTION IF EXISTS py_scale(INTEGER);
DROP FUNCTION

CREATE FUNCTION py_scale(a INTEGER) RETURNS TABLE (value INTEGER)
LANGUAGE python
AS $$return [value * 10 for value in a.materialize_py()]$$;
CREATE FUNCTION

-- @normalize explain_routine_ids
EXPLAIN
SELECT t.x, s.value
FROM (VALUES (1), (2), (3)) AS t(x)
CROSS JOIN LATERAL py_scale(t.x) AS s
ORDER BY 1, 2;
QUERY PLAN
ORDER_BY  (rows=30)
  Sort Key: #0 ASC NULLS LAST, #1 ASC NULLS LAST
  ->  PROJECTION  (rows=30)
    Output: t.x, s.value
    ->  DELIM_JOIN  (rows=30)
      ->  PROJECTION  (rows=3)
        Output: x
        ->  VALUES  (rows=3)
      ->  HASH_JOIN
        Join Type: INNER
        Join Condition: #1 IS NOT DISTINCT FROM #0
        ->  EXTERNAL_TABLE  (rows=100)
          ->  PROJECTION
            Output: __external_arg_1, __corr_1
            ->  CROSS_PRODUCT  (rows=1000)
              ->  DUMMY_SCAN
              ->  DELIM_SCAN  (rows=1)
        ->  DELIM_SCAN

DROP FUNCTION py_scale(INTEGER);
DROP FUNCTION
