---------- Input ----------
CREATE OR
 REPLACE FUNCTION ai_list_files (data_stage STAGE_LOCATION, l INT) RETURNS TABLE (
 stage VARCHAR,
 relative_path VARCHAR,
 path VARCHAR,
 is_dir BOOLEAN,
 size BIGINT,
 mode VARCHAR,
 content_type VARCHAR,
 etag VARCHAR,
 truncated BOOLEAN
) LANGUAGE python HANDLER 'ai_list_files' AS $$return {'path': []}$$
---------- Output ---------
error: 
  --> SQL:3:2
  |
1 | CREATE OR
  | ------ while parsing `CREATE [OR REPLACE] FUNCTION [IF NOT EXISTS] [<database>.][<schema>.]<name>(<arg_name arg_type>, ...) RETURNS <type> LANGUAGE <language> [IMMUTABLE|STABLE|VOLATILE] [STRICT] [SECURITY INVOKER|DEFINER] [HANDLER '<handler>'] [PACKAGES ('pkg', ...)] [IMPORTS ('uri', ...)] [ROWS <n>] [CAPABILITY PROFILE <profile>] AS <code>`
2 |  REPLACE FUNCTION ai_list_files (data_stage STAGE_LOCATION, l INT) RETURNS TABLE (
3 |  stage VARCHAR,
  |  ^^^^^ unexpected `stage`. it's reserved keyword, you may avoid using it, expecting <LiteralString>, <Ident>, `IDENTIFIER`, or `)`


