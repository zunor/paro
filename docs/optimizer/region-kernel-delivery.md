# Lightweight regional planning

Task base: `96374421d`. Keep staged planning, one committed tree and explicit
semantic barriers. Default promotion and removal of Cascades are separate gates.

## Delivery order

1. Preserve the current Q72 and Q04/Q11 build-direction counterexamples with
   bounded COMPILE/ANALYZE evidence. Do not infer causal milliseconds from a
   different plan or subtraction of cohort medians.
2. Separate relation estimation, physical response evaluation and executable
   construction. Regional transitions consume completed input summaries, not
   newly manufactured child plans. Ordinary selection and regional selection
   share the same cost kernel and resource feasibility rules.
3. Converge region extraction/enumeration and aggregate-grain states. Preserve
   predicate support, exact child choices, null/outer/reduction semantics and
   evaluation fences. Budget fallback is an explicit policy, not completeness.
4. Price only the source work that a legally installed RF can actually avoid.
   Do not discount scan/decode/hash work twice or change semantic row bounds.
   Defer executor-side adaptive build orientation to a separate task.
5. Verify real SQL and independent small-region oracles before measuring the
   Q04/Q11/Q74/Q72 matrix. Preserve wrong results and unsupported contracts.

The first refactoring cohort must not silently bundle a new estimation model.
If model/coverage changes are necessary, record them as separate interventions.
Winner construction must preserve chosen orientation, grouping grain, output
layout and resource feasibility. Complete a small declared enumeration scope
only when the state equivalence/dominance law justifies it; no global optimality
claim follows from a one-winner subset table.

## Measurements

EvidenceId: `region-kernel-v1`. Exploratory controlled implementation pilot,
not a powered default-promotion or DuckDB parity gate. Same SF1 seed as
`relation-convergence-v1`, DuckDB 1.5.5 native SHA256
`85fad85339c7e345eabb66a33a25d8503c3ee8881f9d298fa8499a97617dbf68`.
Four threads, 2GB, verifier off for normal latency and on for correctness.
Normal `PARO_COMPILE_WORK_EVIDENCE=1`, no Detail in timing samples. Three fresh
process blocks, one warmup, two ABBA warm rounds; diagnostics independently
captured once. Q72 timeout 30s. Preserve all samples and typed result validation.
Register control before implementation and probe after clean build; source and
binary identity live in each maintained collector package. Background machine
load prevents certification and is not permission to discard slow samples.

Use the existing collector with owned processes/paths and immutable seed.
Q04 region time <=1ms is an engineering target, not an asserted result. Compare
normal compiler/C1/warm, selected plans, work counts and allocations separately.
Bound ordinary packages at 4MiB/cell, 64MiB/task; no raw event/log archival.
No baseline blessing, result tolerance changes, data cleanup or extra worktrees.
