# Selected local properties

EvidenceId: selected-local-properties-v1. Control: e7074450 (clean re-op).
Registered before collection; implementation is in progress. This is an
engineering pilot, not powered latency/parity or warm non-inferiority evidence.

One owner for exact selected-node executable contracts and derived properties;
one borrowed DAG index for all property consumers. Live facts invalidate only
dependent properties/ancestors; live expression keys, proofs and implementation
availability still invalidate local contracts. The independent frozen oracle
and final executable verifier remain. No change to normalization semantics,
budgets, deadlines, quality policy, costing, grants, execution or timer boundaries.

Reuse and recheck the immutable seed/runtime declarations from
../20260923/construction-bounds-registration.md and its preparation registration:
DuckDB 1.5.5; same native/extension hashes, SQL and seed; 4 threads, 2GB,
generator-declared metadata (not qualifying for engine parity), binary results,
quality search policy, optimizer_verify off, unchanged 30-second optional budget.
PARO_COMPILE_WORK_EVIDENCE=1 is the sole normal timing observer. Detail runs
separately; never use their instrumented times as normal compiler or C1.

One existing checkout and target, clean committed source for both arms. Run
C/P/C/P batches, each with Q04/Q11/Q74, three fresh processes plus one separate
Detail process per query. One warmup and one ABBA round per normal process:
six independent normal blocks per query/arm. Seeds 2026092401 and 2026092402,
10,000 bootstrap draws. No concurrent builds/tests/workloads. Check the first
cold receipt for non-null compile_work before continuing. At most one separate
two-block exploratory cell per query may inform implementation; never pool it.

Compare complete typed results, actual admitted plan/resources, search work
and stop state before equal-work attribution. Report compiler median/P90, C1,
warm and bounded local-contract/property work. Preserve all slow samples and
failures. A compiler or warm median regression >10% requires investigation;
result error, missing compiler receipt, input mismatch or capacity refusal stops
affected collection. No post-hoc threshold changes or retries until green.

Retain <=20MiB of compact campaign/cell evidence, not routine server logs or
raw trace floods. Validate focused independent oracles, optimizer/workspace
tests, strict Clippy, benchmark and compare-only SQL regress. No expected
updates, baseline blessing, unrelated cleanup or new worktree/build copies.
