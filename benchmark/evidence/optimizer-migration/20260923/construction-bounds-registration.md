# Canonical construction, selected evidence and certified bounds

EvidenceId: construction-bounds-v1. Control: b367b3ea, clean re-op.
Registered before implementation and collection. This is an engineering pilot,
not a powered compiler-latency, warm non-inferiority or cross-engine parity gate.

## Interventions

1. Let predicate construction consume canonical scalars and preserve ordered
   relational barriers, eliminating redundant full-tree preparation where the
   shared construction laws suffice. Keep evaluation fences and CTE residuals.
2. Construct selected-node evidence with its exact payload/choice dependencies;
   compose and refresh only affected properties instead of re-inspecting every
   unchanged payload. Keep the independent frozen-DAG oracle and final verifier.
3. Move certified bounds ahead of avoidable recipe/child preparation in their
   admitted continuation domain. A scalar local winner is not a universal
   parent bound. Uncertain intervals, RF response and shared/overlap phases
   without a proof remain unpruned. Never label bounded prefixes complete.

No deadline, search-work budget, quality-policy, cost-model, execution, grant,
verification or timing-boundary changes. Remove experimental implementations
which fail safety or show no defensible benefit; retain negative results.
Record the actual scope of each admitted implementation, not only this intent.

## Collection

Reuse the input/runtime declarations in preparation-registration.md. Recheck
the relocatable seed (256bf479ca982e30c96f9109fb9920d04233c9643b7bcc6a5fd3f5a50554c927),
DuckDB 1.5.5, native/extension identities and SQL before collection. The existing
TPC-DS collector owns servers, processes, snapshots and bounded RunOutput.

Q04/Q11/Q74; four threads; 2GB; binary results; generator-declared metadata;
quality policy; optimizer_verify off; unchanged 30-second optional deadline.
Normal observer PARO_COMPILE_WORK_EVIDENCE=1 only; statement traces and Detail
off. Separate one Detail block per cell. The metadata track is non-qualifying
for engine parity. Check first cold receipt for actual compiler timing.

Use one checkout/target sequentially, clean committed control/probe source,
four batches C/P/C/P. Each visits Q04/Q11/Q74, with three fresh normal blocks,
one warmup and one ABBA round, giving six blocks per query/arm. Seeds:
2026092307 and 2026092308. Block bootstrap: 10,000 draws. Keep all slow samples,
failures and incomplete cells. Do not run builds/tests during collection.
At most one exploratory two-block + one-Detail cell per query may inform the
implementation before the registered final cohort; never pool those samples.

Report compiler median/P90, C1, warm, exact admitted plan/resources, search
counts and stopping state. Detail explains structure, not normal elapsed time.
Changed plans/work invalidate equal-work attribution, not valid measurements.
A compiler/warm median regression >10% requires investigation; missing timing,
result mismatch, identity drift or capacity refusal stops affected collection.
Do not modify thresholds, delete samples or rerun until green.

Retain at most 20MiB of compact evidence, no routine server logs or duplicated
trace streams. Validate focused construction/continuation oracles, optimizer
and workspace tests, check, strict Clippy, benchmark tests and compare-only
SQL regress. No expected updates, baseline blessing or history cleanup.
