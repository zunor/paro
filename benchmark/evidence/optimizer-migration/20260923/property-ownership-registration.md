# Local properties and construction ownership

EvidenceId: `optimizer-property-ownership-20260923-v1`.
Control source: `437c6539`. This is a bounded engineering pilot, not a powered
latency or parity certificate. Register before new collection.

## Ordered interventions

1. Derive quality properties on exact selected nodes, compose them locally,
   and evaluate CTE demand against its selected producer and incoming edges.
   Reuse only unchanged dependencies; retain the independent slow oracle in
   tests and the final executable winner verifier in production.
2. Consolidate normalization/construction ownership. Remove only proven
   repeated whole-tree work, preserving demand, statistics, volatile/error
   semantics, CTE namespaces, and mandatory resource-feasibility alternatives.
3. Use the post-intervention work ledger to choose the physical-subproblem
   ownership consolidation. One owner must retain its preparation, dependency
   state, progress and completion; do not add another winner/price cache.

Budgets, search policy, deadline, costs, grants and execution are unchanged.
Missing evidence stays missing. No semantic, output, or completeness gate may
be weakened. Exact typed results precede any performance interpretation.

## Collection and comparison

Use the maintained TPC-DS collector, the relocatable SF1 seed and declared
DuckDB 1.5.5/native/extension identities in `preparation-registration.md`.
Recheck actual identities before collection. Four threads, 2GB, binary protocol,
generator-declared metadata, quality policy, verifier off, default 30-second
optional deadline; this metadata track cannot certify cross-engine parity.

Normal fresh/cache-miss samples have tracing off and only the registered
`PARO_COMPILE_WORK_EVIDENCE=1` bounded observer. Diagnostic Detail runs are
separate and never supply normal compiler times. Keep the existing compiler,
admission and C1 boundaries. Validate the first normal receipt before continuing.

One post-stage-2 diagnostic per Q04/Q11/Q74 informs stage 3; it is exploratory,
not a control arm. Final C/P/C/P batches each run Q04/Q11/Q74 with three fresh
process blocks, one warmup, one ABBA round, and one separate Detail block per
query. This gives six independent normal blocks per query/arm, using random
seeds 2026092303 and 2026092304 and 10,000 bootstrap draws. Record committed
probe source and saved binary hashes. Use one Cargo target sequentially, with
no concurrent builds/tests during measurement; retain all slow samples.

Report compiler median/P90, C1/warm, exact selected/admitted identities, search
counts/stops and exclusive work. Q74 quality evidence, Q11/Q04 preparation,
and changed-node/reused-property counts are attribution, not promises of saved
milliseconds. Changed plans invalidate same-work attribution, not valid times.
A compiler or warm median regression above 10% requires investigation, not a
new threshold. Failure, drift, missing receipt or capacity refusal stops the
affected collection; preserve the reason rather than retrying until green.

Bound exploratory plus final RunOutputs/captures to 20MiB. Keep no routine
server logs or duplicated event streams. Require affected oracle/engine tests,
full optimizer/workspace tests, strict Clippy, benchmark and high-FD SQL regress.
No expected-result regeneration or baseline blessing is authorized by this pilot.

## Exploratory collector amendment

The first post-stage-2 Q04 cold-planning capture compiled successfully, but its
cell writer refused a record above its registered 10,578-byte limit. Preserve
that CapacityExceeded result; it is not a timing gate pass. Before replacement
collection, select the existing TPC-DS collector for the three exploratory
cells: two normal fresh blocks/one warmup/one ABBA round plus one independent
Detail block each. The extra normal blocks are exploratory and are not pooled
into final control/probe samples. Two blocks are the collector's minimum;
the rejected one-block invocation collected no data. Keep the original final
sampling contract.

Removing the superseded preflight implementation also removes its unused
process diagnostic setting. The single matching expected setting row is
deleted explicitly; no plan/result baseline is regenerated or blessed.
