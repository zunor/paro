# Remaining historical contracts: registered matrix pilot

EvidenceId: `optimizer-migration-20260923-remaining-contracts-v1`.
Registered before collection. This is a finite diagnostic/held-out pilot,
not a powered non-inferiority or first-statement parity certification.

## Intervention

Source implementations: `03937e99` (Memo-owned join cardinality, explicit
unknown ranking priors) and `5106bffe` (total, layout-preserving restriction
normalization shared by initial normalization and native publication).
No search budget, deadline, quality condition, grant coverage or verification
contract is reduced. Unknown ranking priors must not become relation facts.
Volatile/fallible predicates, projections, region facets and context changes
are normalization barriers.

The third historical optimization, deferring unselected physical extraction,
is **not** part of this intervention. Its old opaque closure does not establish
the current pre-admission dependency, physical-verification and identity
contracts. Existing lazy execution-image lowering is not a substitute claim.

## Fixed inputs and protocol

- Clean source before collection; the maintained collector records its exact
  commit, binary hash, build flags, SQL, harness and data identities. One shared
  Cargo target, sequential builds; no task-owned build/test during sampling.
  Expected release `parod` SHA-256 after the two code commits:
  `1babf012f98913d01d4617e39f0d9803e3a976998c96e9ccfd4c5df1060e5285`.
- Immutable relocatable SF1 seed: `/private/tmp/paro-migration-relative.u1PLBV`,
  SHA-256 `256bf479ca982e30c96f9109fb9920d04233c9643b7bcc6a5fd3f5a50554c927`.
- DuckDB declaration: `benchmark/requirements.txt`, **1.5.5**. Expected native
  runtime SHA-256:
  `85fad85339c7e345eabb66a33a25d8503c3ee8881f9d298fa8499a97617dbf68`.
  Loaded built-ins core_functions/icu/json/parquet report version v1.5.5.
- DuckDB SF1 database SHA-256:
  `568e6c75c0c96a3ba82d6f24e8d515dcf2eb3a85640418c1702c2f9ed6fd8ed7`.
  CSV dataset SHA-256:
  `4c73cc53e1567cad44341276a09589c209919e1002a7a59fbbedbf9d8e7532f3`.
  SQL directory SHA-256:
  `04f3f951be5526437e3d22b3da0964b687e2bff7e597f07d9d247bc4338740c4`.
- Four threads, 2 GB, binary results, generator-declared metadata, `quality`
  search policy. No experimental flags or deadline override; the effective
  optional-search envelope remains the source default of 30 seconds. This
  metadata track is not a symmetric cross-engine parity gate.
- Order: Q04, Q11, Q74, verifier **off**, five independent fresh-process
  blocks per query. Then Q04/Q11/Q74 verifier **on**, two blocks each as
  correctness/verification coverage, not a causal verifier-overhead estimate.
  One separate bounded Detail capture per query/configuration. One warmup,
  one ABBA measurement round per block, seed 20260923, 10,000 bootstrap draws.
- Use `benchmark/corpora/tpcds_compare.py`; C1 is normal trace-off occurrence
  zero with a verified cold miss. Compile is normal receipt
  `compiler_elapsed_us`, not EXPLAIN wall time or optimizer-only time. Report
  all compiler values, median/P90, C1/warm, actual grant, identities, search
  counters and stop state. Do not reconstruct missing receipt fields.

## Decisions, capacity and validation

No rerun-until-green or exclusions of slow valid samples. Result/type/bag/order
errors, seed/runtime drift or invalid receipt ownership stop collection for
investigation. A compiler median below 10 or 12 ms is descriptive, not a
latency guarantee. Historical reports are context, not paired code-control
arms; no cross-report causal speedup is asserted.

The host has background services (including Spotlight and MongoDB). Record
interference and retain samples; without isolated, powered sampling mark
performance certification NotCertified. Do not terminate unrelated services.

Six normal and six diagnostic cells, each with its own maintained RunOutput
registration and single immutable capture reference. Total retained campaign
cap 16 MiB, well below the 64 MiB standard ceiling; no raw trace floods or
server logs in Git. Run workspace tests, strict Clippy, benchmark unit tests
and high-FD SQL regress. Compare the failure set and actual bytes with the
pre-task 18-failure control; never update expected output in this task.

## Receipt-coverage correction (before replacement collection)

The first launch omitted `PARO_COMPILE_WORK_EVIDENCE=1`. Its normal receipts
prove SQL/cache/admission association but correctly have `compile_work: null`.
Those samples remain valid C1 observations and are retained as compiler
Uncovered; no diagnostic timer is substituted and no history is overwritten.
The queued run was stopped after the active query completed when this omission
was detected. This is a collection configuration error, not a query failure.

Run the same six registered cells once under distinct `work-q*-{off,on}` run
identities with **PARO_COMPILE_WORK_EVIDENCE=1**. This is the existing bounded
normal-observation switch, not Detail or statement tracing. The same switch
was enabled in the historical normal compiler receipts. Record its effective
value and observer scope in each input manifest; do not claim zero overhead.
Keep all other inputs, sample counts and thresholds unchanged, and do not pool
the covered and uncovered cohorts. One replacement per cell, not retries chosen
by observed timings. Binary/source-code content is unchanged by this amendment.
