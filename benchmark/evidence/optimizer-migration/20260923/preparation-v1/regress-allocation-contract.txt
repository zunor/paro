[ERROR]
[SCRIPT   FILE]: cases/fulltext/fulltext_score_identity.sql
[ROW    NUMBER]: 32
[SQL STATEMENT]: EXPLAIN ANALYZE
SELECT id FROM score_identity
WHERE to_tsvector('simple', content) @@ plainto_tsquery('simple', 'vector database')
ORDER BY ts_rank(to_tsvector('simple', content),
                 plainto_tsquery('simple', 'vector database')) DESC LIMIT 1;
[EXPECT RESULT]:
QUERY PLAN
PIPELINE 0
  SOURCE #0 FULLTEXT_SCAN (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=55
  TRANSFORM #1 PROJECTION (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=56
  SINK #2 CLIENT_RESULT (actual time=<time-range> rows=<rows> loops=<loops>)
PROFILE schema_version=1 query_id=<id> events=<events> parallelism=<threads> workers=<workers> worker_utilization=<ratio> ready_time_us=<us> wait_time_us=<us> wake_coalesce=<count> backpressure=<count> runtime_filter_installed=0 runtime_filter_no_wait=0
MEMORY_PROFILE grant_bytes=<bytes> revoked_bytes=<bytes> revocable_bytes=<bytes> spill_bytes=<bytes> spill_latency_us=<us> yield_latency_us=<us> repartition_depth=0
Execution Time: <time-ms>
Rows Returned: 1
[ACTUAL RESULT]:
QUERY PLAN
PIPELINE 0
  SOURCE #0 FULLTEXT_SCAN (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=44
  TRANSFORM #1 PROJECTION (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=45
  SINK #2 CLIENT_RESULT (actual time=<time-range> rows=<rows> loops=<loops>)
PROFILE schema_version=1 query_id=<id> events=<events> parallelism=<threads> workers=<workers> worker_utilization=<ratio> ready_time_us=<us> wait_time_us=<us> wake_coalesce=<count> backpressure=<count> runtime_filter_installed=0 runtime_filter_no_wait=0
MEMORY_PROFILE grant_bytes=<bytes> revoked_bytes=<bytes> revocable_bytes=<bytes> spill_bytes=<bytes> spill_latency_us=<us> yield_latency_us=<us> repartition_depth=0
Execution Time: <time-ms>
Rows Returned: 1

[ERROR]
[SCRIPT   FILE]: cases/query/select/select_topn_fallback_spill.sql
[ROW    NUMBER]: 64
[SQL STATEMENT]: EXPLAIN ANALYZE
SELECT id
FROM topn_fallback_spill_case
ORDER BY score DESC
LIMIT 3;
[EXPECT RESULT]:
QUERY PLAN
PIPELINE 0
  SOURCE #0 ROWSET_SCAN (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=41
  TRANSFORM #1 PROJECTION (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=42
  SINK #2 TOP_N_BUILD (actual time=<time-range> rows=<rows> loops=<loops>)
PIPELINE 1
  SOURCE #3 TOP_N_EMIT (actual time=<time-range> rows=<rows> loops=<loops>)
  TRANSFORM #4 PROJECTION (actual time=<time-range> rows=<rows> loops=<loops>)
  SINK #5 CLIENT_RESULT (actual time=<time-range> rows=<rows> loops=<loops>)
PROFILE schema_version=1 query_id=<id> events=<events> parallelism=<threads> workers=<workers> worker_utilization=<ratio> ready_time_us=<us> wait_time_us=<us> wake_coalesce=<count> backpressure=<count> runtime_filter_installed=0 runtime_filter_no_wait=0
MEMORY_PROFILE grant_bytes=<bytes> revoked_bytes=<bytes> revocable_bytes=<bytes> spill_bytes=<bytes> spill_latency_us=<us> yield_latency_us=<us> repartition_depth=0
Execution Time: <time-ms>
Rows Returned: 3
[ACTUAL RESULT]:
QUERY PLAN
PIPELINE 0
  SOURCE #0 ROWSET_SCAN (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=32
  TRANSFORM #1 PROJECTION (actual time=<time-range> rows=<rows> loops=<loops>) logical_node_id=33
  SINK #2 TOP_N_BUILD (actual time=<time-range> rows=<rows> loops=<loops>)
PIPELINE 1
  SOURCE #3 TOP_N_EMIT (actual time=<time-range> rows=<rows> loops=<loops>)
  TRANSFORM #4 PROJECTION (actual time=<time-range> rows=<rows> loops=<loops>)
  SINK #5 CLIENT_RESULT (actual time=<time-range> rows=<rows> loops=<loops>)
PROFILE schema_version=1 query_id=<id> events=<events> parallelism=<threads> workers=<workers> worker_utilization=<ratio> ready_time_us=<us> wait_time_us=<us> wake_coalesce=<count> backpressure=<count> runtime_filter_installed=0 runtime_filter_no_wait=0
MEMORY_PROFILE grant_bytes=<bytes> revoked_bytes=<bytes> revocable_bytes=<bytes> spill_bytes=<bytes> spill_latency_us=<us> yield_latency_us=<us> repartition_depth=0
Execution Time: <time-ms>
Rows Returned: 3
