# Joint aggregate-region pilot

EvidenceId: joint-grain-v1. Registered before collection.

Hypothesis: sharing local relation estimates/physical responses and enumerating
aggregation grain together with join subsets removes full-tree alternative
pricing without losing the beneficial partial/final plans. This is a bundled
architecture intervention, not an isolated constant-factor optimization.

Use the maintained tpcds_compare collector. Compare the same release source's
pipeline and quality policies on Q04/Q11/Q74 against DuckDB 1.5.5. Previous
regional-cost-v1 results are context only, not matched before/after evidence.
Normal compiler receipts use PARO_COMPILE_WORK_EVIDENCE=1; normal trace remains
off. Separate Detail processes own structural evidence, never speed claims.

Three fresh blocks per policy/query, one warmup and two ABBA warm rounds per
block, one separate diagnostic block; seed 20260925, four threads, 2GB, binary
results, verifier off for latency, 60s statement limit. Quality optional search
retains its configured 30s default; pipeline uses its bounded region program.
Order: Q04 pipeline/quality, Q11 quality/pipeline, Q74 pipeline/quality.

Reuse the immutable relocatable seed /private/tmp/paro-migration-relative.u1PLBV
and /Users/linjunhong/workspace/tpcds-sf1 DuckDB/CSV inputs. Queries are from the
local DuckDB extension/tpcds/dsdgen/queries checkout. Generator-declared metadata
is an exploratory track, not a symmetric metadata certification. Runtime
DuckDB native SHA256 must match
85fad85339c7e345eabb66a33a25d8503c3ee8881f9d298fa8499a97617dbf68.
Per-cell manifests own exact source/binary/SQL/seed/environment identities.

VM and background system load are observed. This is an exploratory pilot:
three blocks and sequential policy cells cannot certify latency or parity.
A pipeline warm regression greater than 10% against quality triggers plan
inspection, not a relaxed gate. Preserve every slow sample and failure.
Complete typed result/bag/order validation and receipt/package validation are
required. A correctness failure stops the affected collection for diagnosis.

Write outside the source tree under /private/tmp/paro-joint-grain.lwdKs8;
do not change source during collection. Retain bounded standard packages,
at most 2MiB per cell and 12MiB total. Do not archive raw event floods, data
snapshots or free-text server logs. No default promotion, expected updates,
full-search proof, or broad-corpus certification follows from this pilot.
