# Regional expected-work exploratory campaign

EvidenceId: regional-cost-v1. Registered before collection.

Compare the same release binary's quality and pipeline policies against the
declared DuckDB 1.5.5 baseline. The pipeline intervention uses calibrated hash
work, explicit build orientation, whole-support residual activation and bounded
aggregate-region alternatives priced after join enumeration. This is a bundled
architecture intervention, not a single-change causal attribution.

Use the maintained tpcds_compare collector and its RunOutput/receipt validators.
Q04/Q11/Q74, immutable relocatable SF1 seed
`/private/tmp/paro-migration-relative.u1PLBV`, dataset
`/Users/linjunhong/workspace/tpcds-sf1`, generator-declared metadata. Sources,
binary, SQL, seed and effective settings are recorded by each cell. DuckDB native
module SHA256 is declared as
`85fad85339c7e345eabb66a33a25d8503c3ee8881f9d298fa8499a97617dbf68`.

Three fresh process blocks per policy/query, one warmup and two ABBA warm rounds
per block, one separate Detail process. Four workers, 2GB, binary results, 60s
statement timeout, verifier off for latency, normal trace off with bounded
`PARO_COMPILE_WORK_EVIDENCE=1`; optional search limit remains 30s. No pre-touch.
Run Q04 pipeline/quality, Q11 quality/pipeline, Q74 pipeline/quality. Preserve
all slow samples and errors. Validate results/types/bag/order and normal receipt
association after each cell; stop the affected campaign on correctness failure.

Ambient VM/system load is present. Cohorts rotate by query, not by independent
process, so this pilot is NotCertified. A warm regression over 10% against
quality triggers investigation, not baseline replacement. No default promotion
from three queries. Historical numbers are context only, not a before/after
claim. Retain each bounded package once, at most 2MiB/cell and 12MiB total;
server logs and generated data remain outside the repository.

Correctness broadening is separate: pipeline session tests, workspace tests,
default SQL regress, and a verifier-on TPC-DS breadth screen with the maintained
typed result contract. Unsupported resources/statements and absent corpus data
remain explicit blockers to default promotion and removal of Cascades. No
expected-result updates or silently accepted numeric tolerances.
