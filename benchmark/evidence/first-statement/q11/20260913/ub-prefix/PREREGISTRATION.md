# Authorized prefix microbatch: bounded first assay

User authorized prefix/publication-preserving microbatches after the atomic
counterexample. This assay changes only recipe-visit child payload preparation:
lazy immutable Arc<Winner> slots in contiguous storage, integer tuple positions,
drop all slots at yield/return. No speculative synthesis, batch budget reservation,
cost/frontier/rule change, or new stopping policy. Existing admission/publication
is the microbatch boundary contract. Facts/grants/merge validation remains on
every resumed visit; nothing is cached across visits.

This tests repeated child lookup preparation, NOT the original whole-expression
one-registration proposal. Existing code already hoists the recipe read context;
this assay cannot prove all protocol work eliminated if it is negative.

Before measured queries, use clean committed control cd8020e1 and probe containing
this implementation. Fixed C2→P2→P2→C2 reports, original Q11/data,4 workers/2GB,
handoff1, compile scalars1, E1off/traceoff/first miss/typed ordered90; one separately
timed diagnostic and oracle/report. Warmup1, measured ABBAround1. No concurrent
tests/builds during measured blocks. Archive all slow samples and hashes.

Compare exact synthesis counts and diagnosed admitted fingerprint first. With
equal N1691 and same policy/context, paired normal residual R=optimizer−rules
can estimate saved time per synthesis. A conditional U proxy is
20.4139µs−(R_control−R_probe)/1691; reaching12 requires≥14.228ms savings.
This assumes unchanged non-synthesis intercept and is NOT a new independent
two-N fit. Report raw R and uncertainty, never call R/N the marginal U.
Small pilot is not formal W NI/parity. Continue to larger campaign only on a
credible mechanism effect; otherwise retain negative evidence and withdraw the
unhelpful preparation change. Do not close all protocol hypotheses from this
narrow assay, and do not expand to domain identity or execution work.
