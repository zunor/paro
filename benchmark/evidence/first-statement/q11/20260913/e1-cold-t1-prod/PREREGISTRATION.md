# E1-COLD / T1-PROD

Registered before this round's fresh measurements, 2026-09-13.

Independent recomputation from original T1 per-occurrence normal records:
handoff residual medians 106994 / 41424.5 us, N4903 /1691 give U20.41391656us,
F6.90456709ms; default839278 /585179us, N20340 /7351 give U19.56262992us,
F441.37410748ms. Default witness hits218789, skips171401, bindings27137 are
identical. Handoff hits525/491, skips518/484, bindings331/317 differ: its fitted
intercept is not a demonstrated invariant work component. Two points fit exactly;
neither a constant marginal cost nor a causal identity for F is proved. Prior
ratio-only rejection was too strong. User explicitly lifted old section6.3.

E1-COLD baseline: clean committed b594e968, same archived original Q11 / SQL seed /
4 threads /2GB /binary /generator-declared /random seed1 /bootstrap200 as T-CWC.
4 fresh handoff blocks, trace off and cache miss, full typed90/order validation,
warmup1 + ABBA round1. PARO_COMPILE_WORK_EVIDENCE=1, handoff=1 unchanged.
Stop E1 if same-block C1-W-compiler residual median <15ms. This residual is not
an exclusive phase timer. Do not mix diagnostic portal durations with normal C1.

If reproduced, add bounded opt-in scalar observations for buffer fills, process
faults/RSS, decoder construction, and operator initialization. Read after timer,
but collect inside actual work; do not shift initialization outside C1. Warm
occurrences need their own measurement, not reuse cold evidence. Process-wide
counts require isolated single-query server; disclose scope and maxRSS high-water
semantics. Counts cannot be converted to milliseconds using an invented ratio;
inclusive parallel CPU timers cannot be summed as critical-path wall time.
Four-bucket >=80% gate only with mutually exclusive attributable cold extra wall
time. Otherwise report incomplete, do not assign unknown residual.

Instrumented/off same binary: 4 fresh handoff blocks per arm, retain all valid
slow samples; screen median shift >2ms, then reduce collection before proceeding.
Performance runs/builds/profile do not overlap. No executor algorithm changes.

T1-PROD is separate: frozen actual resource expectation shared with cache key,
mandatory all-class safe plans, explicit optional status, unchanged admission
comparison. Separate commit/campaign; final fingerprint must equal matched control,
handoff synthesis<=2000, C1<=210ms, W noninferior. No E1 instrumentation required
for its normal timing. No claim of ProofComplete or parity. Preserve baseline4
failures and user mixed work. No F/domain identity/allocation/executor optimization.
