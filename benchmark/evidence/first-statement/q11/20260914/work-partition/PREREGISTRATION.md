# Exclusive optimizer partition

Before measurement: retain budget256, rules/model/T1 grant/handoff policy.
Original archived 2418-byte Q11, corpus hash
a1f151c2d5617427a9394f1353d47478682e4d0b79bc16cddcd8be4de438d503.
Same seed72ccb3dc02ce127f61c06b85920a0e75c17a55a60ee341d267251c67611f62b1,
4 threads/2GB and generator-declared metadata (inherited asymmetric declared
keys remain disclosed). No statement trace in primary partition cohort.

Clean committed build, serial OFF/ON/OFF, two fresh blocks each, one warmup
and one measured warm execution, one separate heavy diagnostic per report.
Keep all valid slow samples. This pilot cannot certify W noninferiority/parity.
Require full typed/order90, cache miss and all harness identity checks.
Compare ON optimizer median against the pooled OFF blocks: >10% increase
requires reducing instrumentation and repeating; do not interpret invalid data.

PARO_DIAGNOSTIC_WORK_PARTITION enables a thread-local, nested exclusive ledger.
Same start/end Instants as compile-work optimizer scalar; all intervals including
rejection/error paths sum exactly to total. Unclassified remains explicit.
JSON is buffered and written after optimizer, INSIDE diagnostic compiler/C1.
Serialization is not a normal optimization gain. Old P1 snapshot/phase timers
are off. Scope entry counts include nested/early-return calls, not unique tasks.

B0 preparation; B1 logical loop/task orchestration; B2 root dispatch and binding
matching (including matching requested by scheduling); B3 apply and rollback;
B4 insert/commit; B5 logical scheduling/ancestor enqueue; B6 implementation and
recipe construction; B7 physical subproblem driver including exact tuple/cache
bookkeeping outside nested buckets; B8 synthesis; B9 candidate admission;
B10 publication and physical dependency registration; B11 quality/freeze;
B12 verification/extraction/reporting. B7 is not solely recursive child lookup.
No catch-all is relabeled as scheduling. >=90% classified is required.
Wall-time attribution is not CPU time; no sampling estimates fill gaps.

Report per-entry costs and existing business counter denominators with scope
caveats. B5+B10=25–35ms is a hypothesis. Conditional implementation requires
one bucket >=40% of the previously unclassified work and a concrete removable
operation, preserving workload/choices/fingerprint and completion semantics.
Width4 observation is overfit and not an admissible optimization: logical
counts changed despite same Q11 winner. No width experiment this round.
